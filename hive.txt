Data Analysis
Hive(DWH)--DATA WAREHOUSE TOOL
Running SQL Queries on HDFS for analysis.

Multiple Source(file,rdbms,...)Unstructured----------->DWHTool/hive,Structured/Processed data---USED--FOR----->DATA ANALYSIS

Hive doesnt have storage,it stores only metadata of data 
Hive is Tabular,like RDBMS,database,table,rows,columns,
all datatypes are similar as RDBMS..int,string,timestamp,...
no constraints supported,e.g. pk,fk,unique,not null...
all hive queries should be run on hive terminal
    to active hive terminal on emr-COMMAND:hive OR hive shell
 
Hive__
      |_Derby DBMS(optional,MySQL,etc.)...stores metadata of DATABASE,TABLES,COLUMNS
           |
           |
    points to HDFS
           |
HDFS<------|

SYNTAX: |__create table tble....__|__location...__|
         |__thisis fetched from metadeta__|__this is fetched from location__|

SUPPOSE
HDFS--->cust.txt
/cust/cust.txt

cust.txt
cid,cname,city
1,karan,pune
2,rohan,pune
3,ganesh,mumbai
4,roha,surat
5,rosh,surat

CREATE TABLE HIVE COMMAND:
create external table cust (cid int,cname string,city string)
row format delimited
fields terminated by ','
location '/cust/';

Note:path till folder,no need for full path,difference between internal/external table scroll down

Now u can run any queries on hive table created above. Note:99% queries is like SQL

HIVE Queries

show tables;
desc <table_name>;
show create table <table_name>;
    Shows how table is created with details
select * from <table_name>;
    Structure of flow of query
    select * from cust where cname=karan;----Goes through parser/compiler----HIVE PARSER/COMPILER--------checks the metadeta------>Hive Metadeta
                                                                                            |<-----gives back acknowledgment------------|    
    after acknowledgment query gets coverted in executable format,like mapreduce job internally
    goes through execution engine/executor.-----output----->1 karan pune
load data inpath '<path>' into table cust_in;
    this will load data from <path>,like '/cust.txt' into internal table cust_in.
load data inpath '/home/hadoop/cust.txt' into table cust_in;
    loading data through local file system.
alter table cust rename to cust_bkp;
    table cust will be renamed as cust_bkp

DDL 
    create
    truncate
    drop
    alter
    *condition:some will work on internal and not external
DML 
    not supported in hive
    *condition:storage format should be .orc and transactional property should be enabled
DRL 
    all drl 99% sql queris supported

Difference Internal | external
HDFS data store managed by hive | managed/defined by user
path /user/hive/warehouse/<table_name> | /cust/<table_name> (from above example)
Command
INTERNAL:
create table cust (cid int,cname string,city string)
row format delimited
fields terminated by ',';
______________________________________________________________
EXTERNAL:
create external table cust (cid int,cname string,city string)
row format delimited
fields terminated by ','
location '/cust/';
truncate can be | cannot
drop can be | only hive table delete,not the data on HDFS

#partition
Problem specification:If u have partition data by city,while getting data only for pune will check only pune partition...hence performance improved
is a performace optimization technique,storing data in partitions.
whenever following partitiing commands os performed data will get stored in parts.
Lets see practically...
COMMAND:
# internal table
create table cust (cid int,cname string,city string)
row format delimited
fields terminated by '|';
#for partition
create table cust (cid int,cname string)
partition by (city string)
row format delimited
fields terminated by '|';
data of city will be saved in partition with different location by city
#for loading data in partition table
2 methods(static and dynamic)
`#static partition(enabled default):used to bulk load(i.e.large data)
 here we need to load 1 by 1 partition manually
 FOR STATIC PARTITION U NEED SEPARATE FILES FOR SEPARATE PARTITION (e.g.Pune.txt,Mumbai.txt,Surat.txt...)
COMMANDS:
load data local inpath '/home/hadoop/pune.txt' into cust_p  partition(city='Pune');
load data local inpath '/home/hadoop/mumbai.txt' into cust_p  partition(city='Mumbai');
load data local inpath '/home/hadoop/surat.txt' into cust_p  partition(city='Surat');
    if files are on HDFS use only (..data inpath '...)
    after execution Pune folder will created and record will get stored inside it.
    user/hive/warehouse/cust_p/city=Pune/pune.txt
    user/hive/warehouse/cust_p/city=Mumbai/mumbai.txt
    user/hive/warehouse/cust_p/city=Surat/surat.txt
                             
show partition cust_p;
output:
    OK
    city=Pune
    city=Mumbai
    city=Surat

`#dynamic partition
FOR FOR DYNAMIC PARTITION U NEED  FILE FOR  PARTITION (e.g.cust_p.txt[which includes data of all cities])
Steps:1)Create non-partition table load data inside it
2)insert data from non-partitoin table to partition table

COMMANDS:
load data local inpath '/home/hadoop/cust.txt' into cust_np;

Lets create partition table:
create table cust_dp (cid int,cname string)
partition  by (city string)
row format delimited
fields terminated by '|';

insert into cust_dp partition(city) select * from cust_np;

----------------------------------------------------------------
PARTIOTN ON MULTIPLE COLUMNS
U CAN SUBDIVDE PARTITIONS IN MORE PARTITIONS,CREATING HEREARCHIHAL STRUCTURE
#static way
cust_mh_pune.txt,cust_mh_mumbai.txt,cust_gj_surat.txt

final table:cust_smp columns:cid,cname,state,city

output:
OK
state=GJ/city=Surat
state=MH/city=Pune
state=MH/city=Mumbai

#dynamic way
load data local inpath '' into table cust_tmp

create table cust_dmp (cid int,cname string)
partition  by (state string,city string)
row format delimited
fields terminated by '|';

insert into table cust_dmp partition(state,city) select * from cust_tmp;
output:same as above

===================================================================================
DataWarehouse | Datalake
Structured data | Unstructured data
ETL | ELT

OLTP | OLAP
OnLine Transactional Processing | OnLine Analysis Processing
MySQL,other databases | Hive

read on schema | write on schema
check schema on read | check schema on write
hive | MySQL,etc.
read on schema ,doesnt check schema during write operation,like 'Ten' in Column datatype int,but will show erro but will show NULL while reading it.
while write on schema will check schema,datatypes during write operation itself.
===========================================================================================
#Bucketing
bucketing is similar to partition but difference is when column have too many variety of data,for e.g.Mobile no.
partitioning mobile number will create separate folder for each record,instead we can use bucketing
bucketing creates files.(bucketing create files,partiton create folders)
when to use:
    1.not particualr column for partition
    2.unequal partition size
Bucketing works on hashing technique,u just have to determine number of buckets
buckets are determined using modulous(%)i.e. reminder after division
https://www.javatpoint.com/bucketing-in-hive
e.g.
cust 
cid cname
1%4 = 1
2%4 = 2
3%4   3
----
4%4   0
5%4   1
6%4   2
-------
7%4   3
8%4   0
-----------
9%4   1
10%4  2


1 5 9
-----
2 6 10
----------
3 7
----------
4 8


COMMAND:
create table cust_b(cid int,cname string)
clustered by (cid) into 4 buckets
row format delimited
fields terminated by '|';

insert into cust_b select * from cust;
(given cust have all customer data seprated by pipe'|')

Note:bucketing is not limited to numerical column,even string can be bucketed using hashing technique

--------
So for performance optimizing we seen
1)partitioning
2)bucketing
3)while running query there will be execution engine Mapreduce,on amazon tez is default execution engine used
Execution engines:MapReduce,Tez,Spark
MR is slower as compare to others
COMMAND:
set hive.execution.engine=tez;
4)fileformat:text,rc,orc,avro,parquet,sequence
orc is faster,text is default
COMMAND:
create table cust_tt (cid int,cname string)
row format delimited
fields terminated by '|'
stored as ORC;
*options(rcFile,avro,parquet,sequenceFile)
orc(optimized row column):it creates its own index,hence faster.

*serde:serde is a java program to read particular format of data which is added by default with .jar with hive
purpose:serialization and deserailization.
location of jar:
/usr/lib/hive/lib/
4)compression technique
COMMAND:
create table cust_parquetsnappy (cid int,cname string)
row format delimited
fields terminated by '|'
stored as parquet
TBLPROPERTIES('COMPRESS'='snappy');

insert into cust_parquetsnappy select * from cust;

Till now we were loading record without header,i.e. column table_name
we will see how to add header next...

